'use strict';

/**
 * Rendering context types.
 */
module.exports = {
	/**
	 * Canvas rendering context type.
	 * @constant {Symbol}
	 */
	CANVAS: Symbol(),

	/**
	 * WebGL rendering context type.
	 * @constant {Symbol}
	 */
	WEBGL: Symbol()
};
